# Dev defaults (committed): local Hub + local anvil chain.
# scripts/dev-chain.mts writes .env.development.local with the deployed addresses,
# which takes precedence over this file in `vite dev`.
VITE_API_URL=http://localhost:8787
VITE_CHAIN_ID=31337
VITE_RPC_URL=http://127.0.0.1:8545
